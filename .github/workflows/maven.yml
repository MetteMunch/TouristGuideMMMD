
name: Java CI with Maven

on:
  push:
    branches: [ "featureMMH2025" ]
  pull_request:
    branches: [ "featureMMH2025" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4.3.0
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B test


